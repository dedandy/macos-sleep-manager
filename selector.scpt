# Sovrascrive il selettore con una riga singola e pulita
echo 'POSIX path of (choose file with prompt "Seleziona App" of directory "/Applications")' > ~/.selector.scpt

# Forza i permessi corretti
chmod 644 ~/.selector.scpt